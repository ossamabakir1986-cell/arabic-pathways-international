/**
 * Arabic Pathways — International configuration.
 *
 * Keep API keys OUT of this file. The browser talks only to the Apps Script
 * gateway. When you are ready to connect the live gateway, paste the existing
 * deployed /exec URL below.
 */
window.ARABIC_PATHWAYS_CONFIG = {
  gatewayUrl: '',
  gatewayExpectedService: 'Pathways Gateway v5.1',
  voiceFallback: true
};
